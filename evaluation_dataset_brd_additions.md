# 评测集平台 BRD 增量需求（侧线讨论整理）

> 用途：供主线更新 `evaluation_dataset_platform_brd.md` 时参考（增量内容已并入 BRD **V1.3**，本文件作为决策依据存档）。
> 来源：侧线讨论「EIU 抽取、覆盖率、质量评估、P0 机制、证据溯源」收口结论。
> 关联文件：
> - 四项关键决策（上传评测集模板、公共库维护、覆盖率适用范围、量化评估与门禁关系）：[evaluation_dataset_discussion_summary.md](evaluation_dataset_discussion_summary.md)
> - 评分策略与质量评估研究依据：[evaluation_scoring_strategy.md](evaluation_scoring_strategy.md)

---

## 1. 高关联文档的 EIU 抽取（上下文补全机制）

### 1.1 核心原则

- EIU 的"独立"指**可独立判定真假**（仅凭 EIU 陈述 + 引用证据可验证），不是"脱离上下文可读"，也不是"位于同一自然段"。
- 高关联、跨页、跨自然段的知识点**不是合并或拆碎的理由**，也不是独立性判断失败的理由；"引用未展开"才是失败原因。
- 上下文补全/物化：把真值条件写进 EIU 陈述，宁可陈述长一点；补全**不新增 EIU 数量、不重复计覆盖率**。

### 1.2 三类上下文的处理方式

| 上下文类型 | 处理方式 |
|---|---|
| 真值条件（主体、条件、范围、期间、币种单位、例外） | **吸收**：补全进 EIU 陈述（物化） |
| 背景信息（章节主题、文档背景、行业常识） | **外置**：放入上下文说明层（FR-SEM-001），不参与 EIU 判定 |
| 可独立判定的业务陈述（定义、范围条款、阈值本身） | **单独成 EIU**，与其他 EIU 建立关系（FR-SEM-007） |

### 1.3 抽取流程新增步骤

1. 前置构建**上下文清单**：先把文档级全局条款（适用对象、期间、术语定义、章节范围）抽成 EIU 或术语记录，作为后续补全的引用源；
2. 逐块抽取候选 EIU 后执行**上下文充分性检查**：仅凭"EIU 陈述 + 证据"能否判定真假；不能则从上下文清单取回限定语补进陈述，或补充指向限定条款的证据引用；
3. 补全后重新检查，仍不能判定的：说明拆错或确实依赖另一条 EIU，此时登记依赖关系（`condition_of` / `references`），**不得把两条 EIU 揉成一条**；
4. EIU 证据范围可跨块（见第 2 节）；
5. 需要组合多个 EIU 才能回答的题走跨段/跨文档多跳路径（8.7），不在抽取阶段预合并。

### 1.4 拆与合的判定标准

- **拆**：两条陈述可分别被证明/否定 → 拆；一条限定语同时作用于多条规则 → 每条 EIU 各自补全该限定语（或引用），不另计；
- **合**：必须合在一起才能判定真假的才合（如"A 和 B 共同构成 X 的完整定义"）；
- 既有规则不变：例外单列、定义与公式分列、表格业务行、重复合并、不可回答排除（FR-SEM-004）。

### 1.5 双通道校验新增检查项

FR-SEM-005 对高关联文档增加**"上下文自足性"检查**，防两类失真：

- **过度合并**：一串关联内容包成一条大而空、无法验证的 EIU；
- **过度拆分**：上下文剥离后产生歧义或错误归属的 EIU。

不满足的判 `revise`，并注明是"缺限定语"还是"需要补关系"。

**BRD 落点**：FR-SEM-001/003/004/005，新增小节或扩展规则。

---

## 2. 跨页/非连续知识点的证据溯源

### 2.1 总体原则：存储与展示分层

EIU 证据分两层设计，**存储/审计层**保留机器可审计的全部字段，**审查视图层**面向用户阅读，默认只展示人话，两者不混用：

- **存储/审计层**：有序证据列表，每条是全字段原文定位（复用 FR-PARSE-003：`document_id`、`document_version`、`section_path`、`page_no`、`block_id`、原文起止偏移/坐标、原文片段、解析器版本）+ 角色 + 版本信息；供审计、增量对比、导出使用；
- **审查视图层**：只展示"原文引用片段 + 位置标签（页码·条款）+ 角色中文名"，全字段折叠在"详情"里；
- 两层共享同一份数据，审查视图只是投影，不额外维护一份简化证据。

### 2.2 存储与审计层（机器视角）

- EIU 的证据是**有序证据列表**，不是"一块原文"；
- 每条证据**引用原文片段，不复述 EIU 陈述**（防止循环溯源）；
- 角色用内部编码存储：`direct`（正文依据）、`qualifier`（条件来源）、`reference`（原文引用关系）、`conflict`（冲突/反证）；
- `statement_provenance`（可选增强）：把 EIU 陈述按片段拆开，映射到对应证据条目（如"适用对象→P1 证据 0、结论→P3 证据 1"），供机器校验"条件从 P1 来、结论从 P3 来"；
- 每条证据带 `document_version` 和 `parser_version`；EIU 带 `evidence_list_version`。

### 2.3 审查视图层（用户视角）

- **默认只显示 L1 可读证据**：原文引用片段 + 位置标签（第 X 页·第 X 章第 X 条）+ 角色中文名；
- **L2 角色与关系**（默认折叠）：跨页时画连线，如"条件来源 第1页 → 正文依据 第3页"；
- **L3 全字段审计**（点"详情"才显示）：块编号、偏移量、解析器版本、证据列表版本等；
- 角色中文标签示例：`direct` → "正文依据"、`qualifier` → "条件来源"、`reference` → "引用关系"、`conflict` → "冲突说明"；
- 审查视图示例：

```text
小微企业在申请流动资金贷款时，资产负债率原则上不得超过70%

证据（3条）：
├─ 条件来源：第1页·第一章第二条「本细则适用于辖内小微企业流动资金贷款」
├─ 正文依据：第3页·第五章第二十一条「资产负债率原则上不得超过70%」
└─ 例外说明：第3页·第五章第二十一条「由政策性担保机构全额担保的，可放宽至75%」
[展开详情：块编号、偏移量、解析版本…]
```

### 2.4 补全必可溯源（硬规则）

上下文补全**不得无证据**：陈述中每个补进来的限定语，必须在证据列表中找到对应块。找不到 → 视为模型臆造，判 `revise` 或 `block`。

### 2.5 版本与增量

- 文档更新后按证据条目对比变化（哪条失效、哪个限定语被替代），支持精确增量定位（FR-DS-INC-001）；
- 历史版本证据引用不被改写。

### 2.6 下游 QA 层继承

- `EvalCase.evidence` **继承 EIU 的完整证据列表**（不是只带结论块）；
- 每个答案要点绑定对应证据（FR-QG-003）；验证时同时满足所有限定前提；
- 失败归因时证据链每一跳可定位原文（机器层），审查时同样以可读视图展示。

### 2.7 导出

- 导出文件（JSON/JSONL）保留**存储层全字段**，供下游独立验证与审计；审查视图不改变导出结构。

**BRD 落点**：FR-SEM-003（存储层证据列表化 + 审查视图分层）、FR-SEM-005（补全可溯源规则）、FR-QG-003（证据继承）、FR-DS-INC-001、8.13（展示视图）。

---

## 3. EIU 质量评估的定位

### 3.1 结论

- EIU 质量评估**不能整体跳过**：QA 评估检查"产品合格"，EIU 评估检查"原料合格"，QA 通过率无法替代（EIU 拆分错误、真值条件缺失、分母脏时，QA 仍可 100% 通过）；
- "通过质量评估的 QA 对占比"是**流水线良率指标**，不是评测集质量的完整定义。

### 3.2 EIU 质量评估瘦身为四项必查

| 必查项 | 作用 |
|---|---|
| 完整性 | 真值条件（主体/条件/范围/期间/单位/例外）是否齐全 |
| 独立性 | 是否依赖未声明的上下文才能判定真假（见第 1 节） |
| 重复率 | 分母去重，防止覆盖率虚高 |
| 可生成性 | 是否适合出题（排除不可回答项，决定是否进分母） |

其余软指标（信息密度、覆盖范围评分等）降为**参考指标**，只展示不设门禁。

### 3.3 数据集质量报告分层

1. 文档→EIU 层：实质文段对账率、EIU 完整性通过率；
2. EIU→QA 层：加权覆盖率；
3. QA 层：QA 通过率、重复率。

**BRD 落点**：FR-SEM-003/005、FR-QA-001、FR-METRIC-001，新增质量评估指标与报告分层。

---

## 4. EIU→文档覆盖层补强

### 4.1 现状与缺口

现状只有"实质文段对账率 = 100%"二值门禁（FR-COVER-002、FR-SEM-005-7），存在三个薄弱点：

1. 对账率是**文段级**的，看不出"拆完整了"还是"只碰了一个点"；
2. "实质文段"判断依赖抽取系统，误判为过渡句就漏出对账范围；
3. 排除项直接缩小分母，无量化指标衡量排除异常。

### 4.2 新增机制

- 量化指标：**实质文段 EIU 关联率**（已拆出 EIU 的实质文段比例）；
- P0/P1 所在文段**逐段核对 EIU 是否拆全**（不只查"有没有关联"）；
- **抽样人工复核**估算抽取召回率（随机抽段落核对漏抽，类似检索 Recall 思路）；
- **分母排除率异常阈值**：排除比例异常偏高时强制人工确认。

**BRD 落点**：FR-COVER-001/002/003、FR-METRIC-001、阶段 0 或 14 验收。

---

## 5. P0 判定机制

### 5.1 结论

优先级**不以 LLM 自由语义判断为准**，采用四层机制：

1. **确定性规则为主**：按 EIU 类型 + 预置规则包映射（监管/禁止/阈值/例外/安全边界 → P0），规则编号进 EIU 记录；
2. **LLM 只做辅助分类**：输出 EIU 类型和置信度，不定 P0/P1/P2；
3. **审核 Skill 复核**：对类型判定和优先级映射做差异裁决，不一致进 `revise`；
4. **用户确认兜底**：P0 赋值的新增/下调需用户确认，全程留痕、版本化。

**BRD 落点**：FR-SEM-003、FR-COVER-002、阶段 0（预置规则 + 用户确认）。

---

## 6. P0 用户确认流程

### 6.1 确认时点（常规流程不逐条确认 P0）

1. **规则层**（阶段 0 / 规则变更）：一次性确认 P0/P1/P2 规则包；
2. **版本确认**：抽取 + 生成完成后，结合覆盖率报告、审核结果确认版本；
3. **P0 异常动作**：删除/停用 P0 样本、P0 未覆盖仍想发布、把 P0 移出分母 → 必须人工确认并说明理由；
4. **发布前**最终确认。

### 6.2 建议交互形态：P0 清单视图

- 抽取完成后展示本批次 P0 清单：完整陈述、P0 规则依据、抽取置信度；
- 默认批量通过，用户只处理异常项（误判 P0、漏抽），标记后走 `revise` 或人工裁决；
- 规则变更走版本化流程，不静默改。

### 6.3 硬边界

- 用户确认**不能豁免** P0 覆盖率 100% 门禁（BR-005）和 S0 强制规则；
- P0 降级必须走规则变更流程、留痕、全量重算覆盖率。

**BRD 落点**：4.1 使用对象、FR-SEM-005、FR-COVER-002、FR-DS-EDIT-002、阶段 0。

---

## 7. 与既有收口文件的衔接（主线需一并处理）

以下内容已在 [evaluation_dataset_discussion_summary.md](evaluation_dataset_discussion_summary.md) 中收口，主线更新 BRD 时需与本清单一并写入：

1. **决策 1**：直接上传评测集模板（单轮 `q/a/evidence/dimension`；多轮 `session_id + turns[] + key_turn + depends_on_turns`，类型 `memory`/`coherence`；评分口径：短答案规范化精确匹配、长答案语义相似度 + 固定校准集验证；评分策略为运行侧配置，不进模板字段）；
2. **决策 2**：公共评测集库由组织方预置，用户只能查看和选择使用，不开放共享入库；条目同样经质量评估与治理审核、版本化；维度**暂不固定**，系统做成可配置体系（BRD 不写死"合规安全/性能稳定/准确性/场景可用性"四个示例维度）；Agent 评测前可组合多个来源形成临时标准化评测集；
3. **决策 3**：EIU 覆盖率、85% 门禁、P0 全覆盖**只适用文档生成评测集**；上传评测集和公共库只有 QA 对、不产生 EIU、不参与覆盖率计算；无证据样本不参与证据回溯率统计，展示与导出带降级标注；
4. **决策 4**：三套量化质量评估（EIU / QA / 上传评测集）为**参考评分体系**，界面展示和质量报告使用；质量评分低于阈值时警告并要求用户明确确认，确认后可继续（提示性门禁）；S0 强制规则为不可绕过硬门禁。

另：主线早前从共享对话中识别的增量（三类评测集统一管理、向量化对象从 chunk 改为 EIU、Agent 评测前评测集组合选择、前端展示部分可量化质量评估结果、产品定位升级为"评测数据资产管理 + 自动构建 + Agent 能力评估平台"）与以上内容合并后统一写入 BRD。

---

## 8. 新增/调整的验收与指标（汇总）

- FR-METRIC-001 新增：实质文段 EIU 关联率、EIU 完整性通过率、EIU 独立性通过率、EIU 重复率、分母排除率；
- FR-METRIC-001 保留并明确：QA 通过率 = 流水线良率（不作数据集质量唯一口径）；
- 数据集质量报告分层：文档→EIU → EIU→QA → QA 三层展示；
- 新增门禁类型说明：EIU 质量四项必查（完整性/独立性/重复率/可生成性）为治理参考 + 提示性门禁，S0 为硬门禁；
- 验收标准（14.2）建议补充：跨页 EIU 证据可逐条溯源（存储层全字段 + 审查视图默认只展示原文片段与页码·条款，详情可查全字段）、补全限定语无证据判臆造、P0 清单视图批量确认、EIU 完整性/独立性通过率指标。
