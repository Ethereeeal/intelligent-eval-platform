# 问答生成平台 Demo 技术方案

> 适用范围：基于《智能评测集平台业务需求书 V1.3》，优先支持本地单机 Demo、三人协作开发与后续迭代。
>
> **核心定位：问答生成平台，不是 RAG 问答系统。**

---

## 文档导航

技术方案按模块拆分为 8 份文档（以各模块目录下 README 为准，`docs/tech/` 未另行创建）：

| 文档 | 覆盖 BRD 章节 | Demo 状态 |
|---|---|---|
| [m01 — 数据基础](modules/m01_data_foundation/README.md) | 8.1 语料库与文档接入 / 8.2 结构化解析 / 8.4 存储与索引（EIU 向量化） | 必做 |
| [m02 — EIU 抽取与覆盖规划](modules/m02_eiu_coverage/README.md) | 8.3 语义理解与知识编译 / 8.5 覆盖规划 | 必做 |
| [m03 — 评测集生成](modules/m03_generation/README.md) | 8.6 单段问题生成 / 8.7 跨段 / 8.8 改写与泛化 / 8.11 难度体系 | 必做（仅单段 + 问答对输出，含泛化） |
| [m04 — 质量门禁与治理审核](modules/m04_quality_governance/README.md) | 8.9 问题质量门禁与治理审核 / FR-QA-005 质量评估参考评分（预留） | 必做（5 项基础检查） |
| [m05 — 数据集生命周期](modules/m05_dataset_lifecycle/README.md) | 8.10 版本数据划分 / 8.12 编辑 / 8.13 浏览导出 / 8.14 增量 / 8.15 泛化 / 8.16 回流 / 8.17 空评测集 / 8.22 三类来源统一管理（预留） | 必做（基础版本+导出） |
| [m06 — 评测后数据回流](modules/m06_feedback_loop/README.md) | 9.（仅回流；自动评测不在本平台） | Demo 不做（后续版本） |
| [m07 — 智能问答交互](modules/m07_smart_qa/README.md) | 8.19 智能问答交互 | Demo 不做（后续版本） |

---

## 一句话总结

```
输入一组文档 → 解析Block → 规则分类+LLM抽取EIU（知识点） → EIU向量化（FAISS） → 覆盖规划
→ 选择输出：① 文档知识点（EIU自然语言版本） ② 问答对（支持上传问答对，泛化扩写）
→ 质量校验（量化评估预留） → 版本冻结 → 导出
```

**不是** RAG 问答、不是检索召回、不是上传搜索展示。

**BRD V1.3 方向**：评测集管理统一三类来源（文档生成 / 直接上传 / 公共评测集库），Agent 评测前可勾选维度或合并多来源形成临时标准化评测集；当前 Demo 聚焦文档生成链路，其余来源与组合选择预留后续版本（详见 m05）。

---

## 技术栈

| 层 | Demo 选型 |
|---|---|
| 后端 | Python + FastAPI |
| 数据库 | SQLite |
| 存储 | 本地文件系统 |
| 向量（辅助）| FAISS + BGE-small-zh-v1.5 |
| LLM | OpenAI 兼容 API（可配置） |
| 前端 | 管理台静态页面 |

## 核心数据流

```
corpus → document → block → eiu → eval_case（题目+答案+证据）
                              ├── coverage_report
                              ├── quality_check_result
                              └── dataset_version
```

## Demo 阶段全局边界

**必做：** 文档上传解析 + EIU抽取（规则分类+LLM单通道） + EIU向量化 + 单段题目生成 + 标准答案+证据绑定 + 5项质量校验 + 覆盖率计算 + 版本冻结 + JSONL导出 + 两种输出模式（知识点 / 问答对+泛化，泛化支持上传问答对）

**不做：** 跨文档题目（Demo 不做，方案预留，见 m03 §3.4） + 跨段题目（延后） + 反例对抗（延后） + 双通道校验（FR-SEM-005，Demo 不做 · 后续待讨论，不一定做） + 语义关系抽取（FR-SEM-007，Demo 不做 · 后续也不做，知识图谱已明确不做） + 治理审核Skill（后续版本） + 量化质量评估参考评分（BRD V1.3 FR-QA-005，后续版本） + 直接上传评测集/公共评测集库/评测集组合选择（BRD V1.3 §8.22，后续版本） + 自动评测体系（待测系统适配/指标计算/失败归因/优化建议，均不在本平台） + 评测后数据回流（06，Demo 不做） + 智能问答交互（07，Demo 不做） + 增量更新（Demo 不做，重传采用覆盖式全量重算）

## 三人分工

| 人 | 模块 |
|---|---|
| A | 数据层 + API框架 + EIU抽取 + 覆盖规划 |
| B | 文档解析 + 题目/答案生成 + 质量校验 + Prompt设计 |
| C | 前端页面 + 导出 + 联调 |

## 3天交付计划

| 天 | 目标 |
|---|---|
| Day 1 | 环境 + 文档解析 + EIU抽取跑通 |
| Day 2 | 题目生成 + 答案+证据 + 质量校验跑通 |
| Day 3 | 覆盖率 + 导出 + 前端关键页面 + 演示 |
