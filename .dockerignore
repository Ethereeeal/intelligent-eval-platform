# 不纳入镜像构建上下文，避免把大体积模型权重 / 依赖缓存 / VCS 元数据打进镜像
models/
.git/
.gitignore
.env
.env.*
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/
.codebuddy/

# studio 镜像: 前端已独立为 front-end/ 目录作为构建上下文，无需在项目根排除；
# 后端镜像（context 为项目根）构建时带上 front-end/ 仅为静态资源，体积可控，可保留。
# front-end/
