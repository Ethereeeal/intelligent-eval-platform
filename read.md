# 推送变更记录

本文件记录已成功推送到远端的变更。每次开始任务时先阅读本文件与 `AGENTS.md`；每次推送成功后按时间顺序追加一条记录。不得记录令牌、密码、私有地址或其他敏感配置。

## 2026-08-24｜front-func｜`78dfee7..7092319`

- **提交**：`50a6ef4 docs: 补充评测工作台合并记录`、`7092319 refactor: 拆分评测集生成与展示模块`。
- **变更**：补充评测工作台合并记录；保留单一「评测集库」导航入口，将评测集生成逻辑拆分为 `front-end/js/05-evalset-generate.js`，将展示、上传与组合逻辑拆分为 `front-end/js/08-evalset-library.js`，页面内通过左侧目录切换生成与展示。
- **校验**：相关前端脚本已通过 `node --check`；页面切换已在本地 8080 环境验证；远端 `origin/front-func` 已指向 `7092319`。
