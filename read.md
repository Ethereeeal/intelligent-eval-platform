# 推送变更记录

本文件记录已成功推送到远端的变更。每次开始任务时先阅读本文件与 `AGENTS.md`；每次推送成功后按时间顺序追加一条记录。不得记录令牌、密码、私有地址或其他敏感配置。

## 2026-08-25｜front-func｜`2ddb99e..a5cac44`

- **提交**：`a5cac44 feat: 完善评测运行反馈与报告导出`。
- **变更**：评测发起前增加请求配置与可执行评测集确认、可编辑的默认运行名称；评测结果按可执行评测集版本建立目录，运行中展示完成比例与预计剩余时间；原始报告支持 Excel 全量导出和按当前筛选结果导出。
- **校验**：前端语法检查、后端编译、M08 单元测试（4 项）、Excel 报告读取测试、`git diff --check` 和敏感信息检查通过；Docker Compose 重建成功，确认弹窗、结果目录、进度、预计时间和报告布局经浏览器验证，控制台无错误；远端 `origin/front-func` 已推至 `a5cac44`。

## 2026-08-25｜front-func｜`468b26b..2873bef`

- **提交**：`2873bef fix: 修复中文文档名乱码`。
- **变更**：修复历史上传中文文件名被按 Latin-1 入库造成的文档库与生成库乱码；服务启动时定向迁移可明确还原的文件名，并在上传、预检和重传入口统一规范化 GB18030/UTF-8 文件名。
- **校验**：3 项文件名编码单元测试、Python 字节码编译和 `git diff --check` 通过；Docker Compose 重建成功，`/api/documents` 返回正常中文；文档库、知识点来源文档与生成库页面均经浏览器截图验证，控制台无错误；功能提交已成功推送为 `2873bef`。

## 2026-08-25｜front-func｜`d6f8e50..6a7bc87`

- **提交**：`8635f01 feat: 完善评测集库管理与组合生成`、`6a7bc87 merge: 合并 origin/front-func 概览优化`。
- **变更**：合并远端概览监控看板与流程交互；评测集库管理保留四库目录、组合生成、文档/上传库层级来源和生成库中间产物追溯。上传评测集接口对齐真实后端路径并支持可选 `folder_path` 持久化。
- **校验**：合并后前端脚本与后端模块语法检查、`git diff --check`、`tests/test_m05_demo_hardening.py`（11 项）均通过；远端 `origin/front-func` 已推至 `6a7bc87`。

## 2026-08-24｜front-func｜`e03bd5f..77607b2`

- **提交**：`77607b2 feat: 优化概览监控与交互链路`。
- **变更**：概览收敛为四项平台监控指标；重构平台数据链路为默认评测链路与可切换的生成链路，加入缩放渐变交互；修复评测集图标兼容问题，并统一节点连接、留白与视觉层级。
- **校验**：相关前端脚本通过 `node --check`，`git diff --check` 通过；GitNexus 未提交变更检查为低风险且未影响既有执行流程；Docker Compose 重建成功，并通过本地 8080 浏览器截图、双向交互和控制台无错误验证；远端 `origin/front-func` 已指向 `77607b2`。

## 2026-08-24｜front-func｜`78dfee7..7092319`

- **提交**：`50a6ef4 docs: 补充评测工作台合并记录`、`7092319 refactor: 拆分评测集生成与展示模块`。
- **变更**：补充评测工作台合并记录；保留单一「评测集库」导航入口，将评测集生成逻辑拆分为 `front-end/js/05-evalset-generate.js`，将展示、上传与组合逻辑拆分为 `front-end/js/08-evalset-library.js`，页面内通过左侧目录切换生成与展示。
- **校验**：相关前端脚本已通过 `node --check`；页面切换已在本地 8080 环境验证；远端 `origin/front-func` 已指向 `7092319`。

## 2026-08-24｜front-func｜`68a256b`

- **提交**：`68a256b docs: 增加推送变更记录规范`。
- **变更**：新增 `read.md` 作为发布变更记录；在 `AGENTS.md` 固化任务开始读取记录、推送成功后追加变更与校验信息、禁止记录敏感配置的规则；同步更新生产化状态文档。
- **校验**：文档差异检查通过；GitNexus 变更检测确认未影响代码符号或执行流程（低风险）；远端 `origin/front-func` 已指向 `68a256b`。

## 2026-08-24｜front-func｜`9c5b0a9..e03bd5f`

- **提交**：`9c5b0a9 refactor: 目录化评测集展示`、`e03bd5f feat: 独立文档库管理栏目`。
- **变更**：评测集展示改为左侧目录选择生成库、上传库、公共库与评测集库，删除横向视图栏；生成库以单一「库文档」目录展示全部题集，不再区分泛化/基础问题，保留用户目录和库文档的更多操作；输入文档库移为概览后的独立「文档库管理」栏目。
- **校验**：相关前端脚本通过 `node --check` 与差异格式检查；GitNexus 已核对目录、移动、删除、导出、上传刷新与导航跳转的影响范围；本地 8080 预览已同步。
