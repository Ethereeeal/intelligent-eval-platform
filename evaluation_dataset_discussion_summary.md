# 评测集平台需求讨论收口结论

> 用途：主线程更新 `evaluation_dataset_platform_brd.md` 时的决策依据。
> 状态：四项关键问题已与用户逐项确认，结论如下。

## 决策 1：直接上传评测集的标准模板与评分

### 模板字段（单轮）

| 字段 | 必填 | 说明 |
|---|---|---|
| `q` | 是 | 问题 |
| `a` | 是 | 标准答案；拒答直接写"基于材料拒绝回答" |
| `evidence` | 否 | 来源/证据；不填则标记"无证据样本" |
| `dimension` | 否 | 测试维度归类（维度体系待定） |

明确不含：同义答案（担心歧义）、数值容差、预期行为、intent_id、corpus_id、内容优先级、数据集划分。

### 多轮样本（可选模式）

- 用户可选择是否启用多轮测试；模板为 `session_id` + `turns[]`，最终轮 `a` 必填；
- 中间轮可标记 `key_turn`，测试类型为 `memory`（记忆）或 `coherence`（连贯性），须声明 `depends_on_turns`；
- `memory`：必须依赖前置轮信息，且该信息不得在关键轮问题中重复出现；
- `coherence`：评分时检查与前置轮输出是否自相矛盾；
- 运行记录必须保存完整对话过程，用于归因；
- 至少要有最终轮标准答案；无关键轮时报告标注"仅覆盖最终结果"。

### 评分口径

- 短答案（数值、日期、条款号、固定短语、拒答）→ 规范化精确匹配；
- 自然语言长答案 → 语义相似度评分（embedding / 评分模型 + 阈值），阈值须经固定校准集验证；
- 评分策略为平台运行侧配置，不进模板字段；
- 同义答案由上传者选填的选项已取消，语义评分为自然语言答案的主判方式。

## 决策 2：公共评测集库的内容来源与维护

- **内容来源**：组织方（银行/平台运营）预置内置；用户只能查看和选择使用，不开放用户共享入库（不引入审核者角色，与单用户设定一致）；
- **维护方式**：条目同样经过质量评估与治理审核，版本化管理（新增、更新、停用留痕）；质量评估复用"上传评测集质量评估"指标（数据完整率、重复问题比例、有效 QA 比例、覆盖维度）；
- **维度分类**：暂不固定，待首批公共 QA 数据集确定后再定；系统维度做成可配置体系，BRD 中不写死四个示例维度；
- 组合使用：Agent 评测前可选择单个评测集、勾选公共库维度或合并多个来源形成临时标准化评测集。

## 决策 3：覆盖率与发布门禁的适用范围

- EIU 覆盖率、85% 门禁、P0 全覆盖**只适用于文档生成评测集**；
- 上传评测集和公共评测集库只包含 QA 对、无源文档、不产生 EIU，**不参与 EIU 覆盖率计算**，只做质量评估和组合选择；
- 无证据样本不参与证据回溯率统计，展示与导出带降级标注。

## 决策 4：量化质量评估与审核门禁的关系

- 三套量化质量评估（EIU 质量评估、QA 质量评估、上传评测集质量评估）作为**参考评分体系**，用于界面展示和质量报告；
- **提示性门禁**：质量评分低于阈值时警告并要求用户明确确认，确认后可继续；
- **S0 强制规则**（内容安全、隐私、证据核验等）为不可绕过的硬门禁，质量评分高不能豁免。

## 附：相关文件

- 评分策略与质量评估研究依据：[evaluation_scoring_strategy.md](evaluation_scoring_strategy.md)
- 业务需求书（待更新）：[evaluation_dataset_platform_brd.md](evaluation_dataset_platform_brd.md)
