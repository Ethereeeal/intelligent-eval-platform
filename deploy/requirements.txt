# Web 框架
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0
python-multipart>=0.0.9          # FastAPI 文件上传（UploadFile）必需

# MySQL 存储评测集
sqlalchemy>=2.0.0
pymysql>=1.1.1

# 对象存储（原始文档）
minio>=7.2.0

# 向量（FAISS）
faiss-cpu>=1.8.0
numpy>=1.26.0

# BGE embedding + rerank
sentence-transformers>=3.0.0
torch>=2.2.0                     # sentence-transformers 依赖，自动装 CPU 版

# LLM（OpenAI 兼容 API，用于 EIU 抽取 / 题目生成）
openai>=1.30.0

# 文档解析（按输入类型选装；当前 parser 仅支持 txt/md）
PyMuPDF>=1.24.0                  # PDF（政策 / 财报常见），import 名 fitz
python-docx>=1.1.0               # Word
